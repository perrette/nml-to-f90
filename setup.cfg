# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = nml2f90/_version.py
versionfile_build = nml2f90/_version.py
tag_prefix = 
parentdir_prefix = nml-to-f90-
